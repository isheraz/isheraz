# Supabase Edge Functions Environment Variables
# These secrets are injected into your Edge Functions when running `supabase functions serve` or when deployed.

# Stripe Integration
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
